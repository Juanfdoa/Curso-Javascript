// imprimir la tabal del 10
// definir un limite de la tabla

let base = 10;
let limite = 20;

for (let i = 1; i <= limite; i++) {
    resultado = base * i;

    console.log(base + ' x ' + i + ' = ' + resultado);
    
    
}
